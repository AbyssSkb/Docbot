# Required by the OpenAI client (or your OpenAI-compatible provider).
OPENAI_API_KEY=replace-me

# Optional OpenAI-compatible endpoint. Leave commented for the default endpoint.
# OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_LLM_MODEL=gpt-4o

# Optional: leave empty for MinerU flash_extract; fill it for authenticated extract(model="vlm").
MINERU_API_TOKEN=
